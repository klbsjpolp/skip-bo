{
  "name": "@skipbo/realtime-api",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "tsx watch ./src/local-dev.ts",
    "lint": "eslint src --ext ts --report-unused-disable-directives --max-warnings 0",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage --coverage.reporter=lcovonly --coverage.reporter=text-summary",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.1056.0",
    "@aws-sdk/client-dynamodb": "^3.1056.0",
    "@aws-sdk/lib-dynamodb": "^3.1056.0",
    "@sentry/aws-serverless": "^10.55.0",
    "@skipbo/realtime-core": "workspace:*",
    "ws": "^8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.153",
    "@types/ws": "^8.18.1",
    "@vitest/coverage-v8": "^4.1.7",
    "esbuild": "^0.28.0",
    "tsx": "^4.22.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
